import { useMemo, useState } from 'react'
import { ArrowLeft, Filter, Search } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; import Sidebar from '../../components/Sidebar/Sidebar'; import PageHeader from '../../components/PageHeader/PageHeader'; import { listarMovimentacoesEstoque } from '../../services/estoqueStorage'; import '../../styles/estoque.css'
function d(v:string){return v?v.split('-').reverse().join('/'):'-'}
export default function HistoricoEstoque(){const navigate=useNavigate();const [busca,setBusca]=useState('');const [tipo,setTipo]=useState('todos');const [inicio,setInicio]=useState('');const [fim,setFim]=useState('');const mov=listarMovimentacoesEstoque();const lista=useMemo(()=>mov.filter(m=>{const texto=[m.produtoDescricao,m.produtoCodigo,m.motivo,m.observacao,m.documentoOrigem,m.origem].join(' ').toLowerCase();return(!busca||texto.includes(busca.toLowerCase()))&&(tipo==='todos'||m.tipo===tipo)&&(!inicio||m.data>=inicio)&&(!fim||m.data<=fim)}),[mov,busca,tipo,inicio,fim]);return <div className="dashboard-layout estoque-layout"><Sidebar/><main className="estoque-page"><PageHeader category="Estoque" title="Histórico de estoque" subtitle="Entradas, saídas, ajustes e brindes registrados."/><div className="estoque-actions-bar"><div className="estoque-actions-left"><button className="estoque-action-btn estoque-action-list" onClick={()=>navigate('/estoque')}><ArrowLeft size={25}/></button><div className="estoque-search estoque-search-toolbar"><Search size={18}/><input value={busca} onChange={e=>setBusca(e.target.value)} placeholder="Buscar produto, motivo, documento ou origem"/></div><label className="estoque-history-filter"><Filter size={18}/><select value={tipo} onChange={e=>setTipo(e.target.value)}><option value="todos">Todos os tipos</option><option value="entrada">Entradas</option><option value="saida">Saídas</option><option value="ajuste">Ajustes</option></select></label><input type="date" value={inicio} onChange={e=>setInicio(e.target.value)}/><input type="date" value={fim} onChange={e=>setFim(e.target.value)}/></div></div><section className="estoque-painel estoque-historico-card"><div className="estoque-table-wrap"><table className="estoque-table"><thead><tr><th>Data</th><th>Produto</th><th>Tipo</th><th>Qtd.</th><th>Anterior</th><th>Atual</th><th>Origem</th><th>Motivo</th><th>Observação</th></tr></thead><tbody>{lista.map(m=><tr key={m.id}><td>{d(m.data)} às {m.hora}</td><td><strong>{m.produtoDescricao}</strong><span>{m.produtoCodigo}</span></td><td>{m.tipo}</td><td>{m.quantidade}</td><td>{m.estoqueAnterior}</td><td>{m.estoqueAtual}</td><td>{m.origem}</td><td>{m.motivo}</td><td>{m.observacao||'-'}</td></tr>)}{!lista.length&&<tr><td colSpan={9} className="estoque-empty">Nenhuma movimentação encontrada.</td></tr>}</tbody></table></div></section></main></div>}
